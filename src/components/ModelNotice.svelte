<div class="mb-5 border border-line border-l-[3px] border-l-warn bg-panel px-[18px] py-2.5 text-[11.5px] leading-relaxed text-dim">
  <span class="text-warn">†</span>
  <b class="font-semibold text-ink">These DPS figures come from this lab's formula, not from the game.</b>
  They are reconstructed from <code class="bg-panel2 px-1 text-ink">turretgenerator.lua</code> and sum
  every barrel assembly, on a sector budget constant fitted to in-game cards (×0.4439). The base
  game's own turret card builds most per-type eDPS lines from the <i>first weapon only</i>, so it
  will report different — usually lower — numbers on multi-barrel turrets like the Predator (×6),
  Proton (×7) or Gatling (×5). Expect the shape and the ranking to hold, and the absolute values to
  drift from what the tooltip shows.
  <span class="text-good">‡</span> marks Raw DPS, the one field that should agree.
</div>
